---
// Custom landing page for Wampoon - Based on Docusaurus design
---

<html lang="en">
<head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Wampoon - WAMP Stack Manager</title>
    <meta name="description" content="Wampoon provides a simple and elegant way to manage your WAMP stack. Start, stop, and configure Apache, MariaDB, and PHP with ease." />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            /* Raccoon Night-Mode Theme (Default) */
            --color-primary: #3a8fd0; /* Cool blue */
            --color-primary-dark: #2b7cb8;
            --color-secondary: #58a85e; /* Leaf green */
            --color-accent: #ffb347; /* Warm amber */
            --color-text: #f8f8f2; /* Soft white */
            --color-text-secondary: #6e6e6e; /* Raccoon gray */
            --color-background: #1c1c1e; /* Charcoal black */
            --color-background-secondary: #1b263b; /* Deep navy */
            --color-card: #2d2d30;
            --color-border: #3e3e42;
        }

        [data-theme='light'] {
            /* Light theme - "Wake the raccoon" mode */
            --color-primary: #3a8fd0;
            --color-primary-dark: #2b7cb8;
            --color-secondary: #58a85e;
            --color-accent: #ffb347;
            --color-text: #1c1c1e;
            --color-text-secondary: #4a5568;
            --color-background: #f8f8f2;
            --color-background-secondary: #edf2f7;
            --color-card: #ffffff;
            --color-border: #e2e8f0;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body {
            font-family: 'Nunito Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: var(--color-text);
            background: var(--color-background);
            overflow-x: hidden;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }
        
        /* Header */
        header {
            padding: 1rem 0;
            background: rgba(28, 28, 30, 0.9);
            backdrop-filter: blur(10px);
            position: relative;
            z-index: 10;
            border-bottom: 1px solid var(--color-border);
        }

        [data-theme='light'] header {
            background: rgba(248, 248, 242, 0.9);
        }
        
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--color-text);
            text-decoration: none;
            text-shadow: 0 2px 8px rgba(58, 143, 208, 0.3);
        }
        
        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
            align-items: center;
        }
        
        .nav-links a {
            color: var(--color-text);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .nav-links a:hover {
            color: var(--color-primary);
        }

        /* Theme Toggle Button */
        .theme-toggle {
            background: var(--color-card);
            border: 1px solid var(--color-border);
            color: var(--color-text);
            font-size: 1.2rem;
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 8px;
            transition: all 0.3s;
            margin-left: 1rem;
            position: relative;
        }

        .theme-toggle:hover {
            background: var(--color-primary);
            color: white;
            transform: scale(1.1);
        }

        /* Custom Tooltip */
        .tooltip {
            position: relative;
            display: inline-block;
        }

        .tooltip .tooltip-text {
            visibility: hidden;
            opacity: 0;
            background: var(--color-card);
            border: 1px solid var(--color-border);
            color: var(--color-text);
            text-align: center;
            border-radius: 8px;
            padding: 0.75rem 1rem;
            position: absolute;
            z-index: 1000;
            top: 125%;
            left: 50%;
            transform: translateX(-50%);
            white-space: nowrap;
            font-size: 0.9rem;
            font-weight: 600;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .tooltip .tooltip-text::after {
            content: "";
            position: absolute;
            bottom: 100%;
            left: 50%;
            margin-left: -5px;
            border-width: 5px;
            border-style: solid;
            border-color: transparent transparent var(--color-border) transparent;
        }

        .tooltip:hover .tooltip-text {
            visibility: visible;
            opacity: 1;
            transform: translateX(-50%) translateY(5px);
        }

        /* Raccoon-themed tooltip content */
        .tooltip-text .raccoon-emoji {
            margin-right: 0.5rem;
        }

        /* Animated Paw Print Loading Spinner */
        .paw-loading {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 9999;
            display: none;
        }

        .paw-loading.active {
            display: block;
        }

        .paw-step {
            position: absolute;
            width: 20px;
            height: 20px;
            opacity: 0;
            animation: pawWalk 2s infinite;
        }

        .paw-step:nth-child(1) { left: 0px; animation-delay: 0s; }
        .paw-step:nth-child(2) { left: 30px; animation-delay: 0.5s; }
        .paw-step:nth-child(3) { left: 60px; animation-delay: 1s; }
        .paw-step:nth-child(4) { left: 90px; animation-delay: 1.5s; }

        .paw-step::before {
            content: '🐾';
            font-size: 24px;
            color: var(--color-primary);
            display: block;
            filter: drop-shadow(0 2px 4px rgba(58, 143, 208, 0.5));
        }

        @keyframes pawWalk {
            0%, 20% { opacity: 0; transform: translateY(10px) scale(0.8); }
            50% { opacity: 1; transform: translateY(0) scale(1); }
            80%, 100% { opacity: 0; transform: translateY(-10px) scale(0.8); }
        }

        /* Paw Print Watermarks */
        .feature-card::after {
            content: '🐾';
            position: absolute;
            top: 1rem;
            right: 1rem;
            font-size: 1.2rem;
            opacity: 0.3;
            pointer-events: none;
            color: var(--color-primary);
        }

        .logo-container::after {
            content: '🐾';
            position: absolute;
            bottom: 15px;
            left: 15px;
            font-size: 2rem;
            opacity: 0.6;
            color: var(--color-primary);
            z-index: 1000;
            pointer-events: none;
        }

        /* Hero Section */
        .hero-banner {
            position: relative;
            overflow: hidden;
            padding: 4rem 0;
            background: linear-gradient(135deg, var(--color-background) 0%, var(--color-background-secondary) 100%);
        }

        .hero-banner::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 50%, rgba(58, 143, 208, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(88, 168, 94, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(255, 179, 71, 0.08) 0%, transparent 50%);
            animation: backgroundFloat 20s ease-in-out infinite;
        }

        /* Visible Paw Print Background Pattern */
        .hero-banner::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                radial-gradient(circle 8px at 15% 25%, rgba(58, 143, 208, 0.25) 0%, transparent 50%),
                radial-gradient(circle 5px at 16% 27%, rgba(58, 143, 208, 0.2) 0%, transparent 50%),
                radial-gradient(circle 5px at 18% 26%, rgba(58, 143, 208, 0.2) 0%, transparent 50%),
                radial-gradient(circle 5px at 17% 29%, rgba(58, 143, 208, 0.2) 0%, transparent 50%),
                radial-gradient(circle 4px at 17% 31%, rgba(58, 143, 208, 0.15) 0%, transparent 50%),
                
                radial-gradient(circle 8px at 75% 65%, rgba(88, 168, 94, 0.22) 0%, transparent 50%),
                radial-gradient(circle 5px at 76% 67%, rgba(88, 168, 94, 0.18) 0%, transparent 50%),
                radial-gradient(circle 5px at 78% 66%, rgba(88, 168, 94, 0.18) 0%, transparent 50%),
                radial-gradient(circle 5px at 77% 69%, rgba(88, 168, 94, 0.18) 0%, transparent 50%),
                radial-gradient(circle 4px at 77% 71%, rgba(88, 168, 94, 0.15) 0%, transparent 50%),
                
                radial-gradient(circle 8px at 35% 85%, rgba(255, 179, 71, 0.2) 0%, transparent 50%),
                radial-gradient(circle 5px at 36% 87%, rgba(255, 179, 71, 0.16) 0%, transparent 50%),
                radial-gradient(circle 5px at 38% 86%, rgba(255, 179, 71, 0.16) 0%, transparent 50%),
                radial-gradient(circle 5px at 37% 89%, rgba(255, 179, 71, 0.16) 0%, transparent 50%),
                radial-gradient(circle 4px at 37% 91%, rgba(255, 179, 71, 0.12) 0%, transparent 50%);
            pointer-events: none;
            z-index: 1;
        }

        [data-theme='light'] .hero-banner::before {
            background: 
                radial-gradient(circle at 20% 50%, rgba(58, 143, 208, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(88, 168, 94, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(255, 179, 71, 0.06) 0%, transparent 50%);
        }

        [data-theme='light'] .hero-banner::after {
            opacity: 0.8;
        }

        @keyframes backgroundFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            33% { transform: translateY(-10px) rotate(1deg); }
            66% { transform: translateY(5px) rotate(-1deg); }
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
            margin: 2rem 0;
        }

        .hero-left {
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        .hero-right {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .title-container {
            margin-bottom: 1rem;
        }

        .site-title {
            font-size: 4rem;
            font-weight: 800;
            color: var(--color-text);
            margin-bottom: 0.5rem;
            letter-spacing: -2px;
            font-family: 'Nunito Sans', sans-serif;
            line-height: 1.1;
            text-shadow: 0 4px 12px rgba(58, 143, 208, 0.4);
            animation: fadeInUp 1s ease-out;
        }

        [data-theme='light'] .site-title {
            text-shadow: 0 2px 8px rgba(58, 143, 208, 0.2);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .site-slogan {
            font-size: 1.8rem;
            color: var(--color-primary);
            font-weight: 700;
            letter-spacing: 0.02em;
            margin-bottom: 0;
            text-shadow: 0 2px 8px rgba(58, 143, 208, 0.3);
            background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
        }

        .site-slogan::after {
            content: '🦝';
            position: absolute;
            right: -2.5rem;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.5rem;
            -webkit-text-fill-color: initial;
        }

        [data-theme='light'] .site-slogan {
            background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 1px 4px rgba(58, 143, 208, 0.2);
        }

        .lead-text {
            font-size: 1.3rem;
            color: var(--color-text-secondary);
            line-height: 1.6;
            font-weight: 400;
            margin: 0;
            max-width: 500px;
        }

        .buttons {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .primary-button {
            background: var(--color-primary);
            color: white !important;
            box-shadow: 0 4px 15px rgba(58, 143, 208, 0.4);
        }

        .primary-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(58, 143, 208, 0.6);
            background: var(--color-primary-dark);
            color: white !important;
        }

        .secondary-button {
            border: 2px solid var(--color-secondary);
            color: var(--color-secondary) !important;
            background: transparent;
            backdrop-filter: blur(10px);
        }

        .secondary-button:hover {
            background: var(--color-secondary);
            transform: translateY(-2px);
            color: white !important;
        }

        .logo-container {
            max-width: 400px;
            width: 100%;
            position: relative;
        }

        .logo-placeholder {
            width: 100%;
            height: 370px;
            background: linear-gradient(135deg, var(--color-card), var(--color-background-secondary));
            border: 2px solid var(--color-border);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--color-text);
            font-size: 2.5rem;
            font-weight: 800;
            letter-spacing: -1px;
            box-shadow: 0 20px 40px rgba(58, 143, 208, 0.2);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .logo-placeholder::before {
            content: '🦝';
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 2rem;
            opacity: 0.6;
        }

        .logo-placeholder:hover {
            transform: scale(1.05);
            box-shadow: 0 25px 50px rgba(58, 143, 208, 0.3);
            border-color: var(--color-primary);
        }

        /* Features Section */
        .features {
            background: var(--color-card);
            padding: 6rem 0;
            position: relative;
        }

        .features h2 {
            text-align: center;
            font-size: 3rem;
            margin-bottom: 4rem;
            color: var(--color-text);
            font-weight: 800;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 3rem;
        }

        .feature-card {
            text-align: center;
            padding: 2.5rem 2rem;
            border-radius: 16px;
            background: var(--color-background);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            border: 1px solid var(--color-border);
            position: relative;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 48px rgba(58, 143, 208, 0.4);
            border-color: var(--color-primary);
        }

        .feature-icon {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            display: block;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--color-text);
            font-weight: 700;
        }

        .feature-card p {
            color: var(--color-text-secondary);
            line-height: 1.6;
        }

        /* Footer */
        footer {
            background: #1a202c;
            color: white;
            text-align: center;
            padding: 3rem 0;
        }

        /* Responsive */
        @media screen and (max-width: 996px) {
            .hero-content {
                grid-template-columns: 1fr;
                gap: 3rem;
                text-align: center;
            }
            
            .hero-left {
                order: 1;
                align-items: center;
            }
            
            .hero-right {
                order: 2;
            }
            
            .site-title {
                font-size: 3rem;
            }
            
            .buttons {
                justify-content: center;
            }

            .logo-placeholder {
                height: 250px;
                font-size: 2rem;
            }
        }

        @media screen and (max-width: 768px) {
            .site-title {
                font-size: 2.5rem;
            }
            
            .site-slogan {
                font-size: 1.2rem;
            }
            
            .lead-text {
                font-size: 1.1rem;
            }
            
            .logo-container {
                max-width: 300px;
            }

            .nav-links {
                display: none;
            }

            .hero-banner {
                padding: 2rem 0;
            }

            .logo-placeholder {
                height: 200px;
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Paw Print Loading Spinner -->
    <div class="paw-loading" id="pawLoader">
        <div class="paw-step"></div>
        <div class="paw-step"></div>
        <div class="paw-step"></div>
        <div class="paw-step"></div>
    </div>

    <header>
        <nav class="container">
            <a href="/" class="logo">Wampoon</a>
            <ul class="nav-links">
                <li><a href="/getting-started/">Getting Started</a></li>
                <li><a href="/guides/example/">Guides</a></li>
                <li><a href="https://github.com/wampoon-box/wampoon-box.github.io">GitHub</a></li>
                <li class="tooltip">
                    <button class="theme-toggle">🗑️</button>
                    <span class="tooltip-text" id="tooltip-text">
                        <span class="raccoon-emoji">🦝</span> Send raccoon to bed
                    </span>
                </li>
            </ul>
        </nav>
    </header>

    <main>
        <section class="hero-banner">
            <div class="container">
                <div class="hero-content">
                    <div class="hero-left">
                        <div class="title-container">
                            <div class="site-title">Wampoon</div>
                            <div class="site-slogan">Built for devs. Approved by raccoons.</div>
                        </div>
                        <p class="lead-text">
                            Wampoon provides a simple and elegant way to manage your WAMP stack.
                            Start, stop, and configure Apache, MariaDB, and PHP with ease.
                        </p>
                        <div class="buttons">
                            <a href="/getting-started/" class="btn primary-button">Get Started</a>
                            <a href="/guides/example/" class="btn secondary-button">Learn More</a>
                        </div>
                    </div>
                    <div class="hero-right">
                        <div class="logo-container">
                            <img src="/logo.jpg" alt="Wampoon Logo" class="logo-placeholder">
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="features">
            <div class="container">
                <h2>Why Choose Wampoon?</h2>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon">🚀</div>
                        <h3>Easy Setup</h3>
                        <p>Get your WAMP development environment up and running in minutes with our streamlined installation process.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">⚡</div>
                        <h3>High Performance</h3>
                        <p>Optimized for speed and efficiency, ensuring your development workflow is smooth and productive.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">🔧</div>
                        <h3>Simple Management</h3>
                        <p>Easily start, stop, and configure Apache, MariaDB, and PHP services with our intuitive interface.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">🛠️</div>
                        <h3>Developer Tools</h3>
                        <p>Includes essential development tools and utilities to enhance your local development experience.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">📚</div>
                        <h3>Comprehensive Docs</h3>
                        <p>Detailed documentation and guides to help you make the most of your WAMP stack setup.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">🌟</div>
                        <h3>Open Source</h3>
                        <p>Built with transparency and community collaboration at its core, completely free to use.</p>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025-present Wampoon. Licensed under the MIT License.</p>
        </div>
    </footer>

    <script>
        // Paw loading animation
        function showPawLoading() {
            const loader = document.getElementById('pawLoader');
            if (loader) {
                loader.classList.add('active');
            }
        }

        function hidePawLoading() {
            const loader = document.getElementById('pawLoader');
            if (loader) {
                loader.classList.remove('active');
            }
        }

        // Global theme toggle function - "Wake the raccoon!"
        function toggleTheme() {
            const body = document.body;
            const currentTheme = body.getAttribute('data-theme') || 'dark';
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            // Show paw loading animation
            showPawLoading();
            
            // Small delay for the animation to be visible
            setTimeout(() => {
                body.setAttribute('data-theme', newTheme);
                localStorage.setItem('theme', newTheme);
                updateThemeButton(newTheme);
                
                // Hide loading after theme change
                setTimeout(hidePawLoading, 800);
            }, 200);
        }

        function updateThemeButton(theme) {
            const toggleButton = document.querySelector('.theme-toggle');
            const tooltipText = document.getElementById('tooltip-text');
            
            if (toggleButton) {
                toggleButton.textContent = theme === 'dark' ? '🗑️' : '🛏️';
            }
            
            if (tooltipText) {
                if (theme === 'dark') {
                    tooltipText.innerHTML = '<span class="raccoon-emoji">🦝</span> Send raccoon to bed';
                } else {
                    tooltipText.innerHTML = '<span class="raccoon-emoji">🦝</span> Wake raccoon for night foraging';
                }
            }
        }

        // Initialize theme when page loads
        document.addEventListener('DOMContentLoaded', function() {
            // Load saved theme (default to dark - raccoons are nocturnal!)
            const savedTheme = localStorage.getItem('theme') || 'dark';
            
            document.body.setAttribute('data-theme', savedTheme);
            updateThemeButton(savedTheme);

            // Add click event listener to theme toggle button
            const toggleButton = document.querySelector('.theme-toggle');
            if (toggleButton) {
                toggleButton.addEventListener('click', toggleTheme);
            }
        });
    </script>
</body>
</html>